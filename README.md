# Spot Vision — website

Static landing page for spotvision.io, hosted on Firebase Hosting
(project `spotvision-patterns2`, site `spotvision-landing`).

Deploy:  `firebase deploy --only hosting --project spotvision-patterns2`
